//Every JSON object will need a string specifying the tag description and all of the containing content.

//It seems like each case study, using this model, will needs its own JSON file.  One JSON object can point to the next JSON object to retrieve the case study data, like primary keys.